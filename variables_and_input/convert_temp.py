# Asking the user to input a temperature in Celsius and initializing the variable
temp_c = float(input("Please enter a temperature in degrees Celsius: "))

# Formula to convert Celsius to Fahrenheit
temp_f = (temp_c * 1.8) + 32

print(temp_f)
